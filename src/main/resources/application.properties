spring.application.name=backend-app

server.port=${PORT:8080}

# Database
spring.datasource.url=${DB_URL}
#spring.datasource.url=jdbc:postgresql://ep-odd-star-ac2dkppv-pooler.sa-east-1.aws.neon.tech/bdneon?user=bdneon_owner&password=npg_tKjGBfL02JFd&sslmode=require&channelBinding=require
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true
# SSL Configuration (opcional, mas recomendado para Neon)
#spring.datasource.hikari.ssl=true
#spring.datasource.hikari.sslmode=require

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# Especificar o driver PostgreSQL explicitamente
spring.flyway.driver-class-name=org.postgresql.Driver
